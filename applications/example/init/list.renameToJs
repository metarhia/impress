application.state.data.car = {
  name: 'name',
  speed: 0
};

setInterval(() => {
  console.debug(
    'Data: ' + application.nodeId + ' ' +
    api.json.stringify(application.state.data.car)
  );
}, 10000);
