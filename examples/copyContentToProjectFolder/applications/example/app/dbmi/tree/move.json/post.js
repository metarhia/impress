module.exports = function(client, callback) {

  /*
  jb.idMove(jb.oid(client.fields.id), jb.oid(client.fields.ref), +client.fields.position, function(err, count) {
    client.context.data = { status: (count>0) ? 1 : 0 };
    callback();
  });
  */

  callback();

}