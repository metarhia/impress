if (api.cluster.isWorker) {

  setInterval(function() {
    console.dir({
      files: application.cache.files, // Object.keys(
      //static: application.cache.static,
      //folders: application.cache.folders
    });
  }, 5000);

}
