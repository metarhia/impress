module.exports = function(client, callback) {

	/*
	jb.idMove(jb.oid(client.req.fields.id), jb.oid(client.req.fields.ref), +client.req.fields.position, function(err, count) {
		client.context.data = { status: (count>0) ? 1 : 0 };
		callback();
	});
	*/

	callback();

}